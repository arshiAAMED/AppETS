import 'package:flutter/material.dart';

class BIXI extends StatelessWidget {
  const BIXI({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.red.shade400,
        title: Text('Horaire BIXI'),
      ),
      body: Center(
      ),
    );
  }
}
